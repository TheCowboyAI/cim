//! CLI module
//!
//! This module provides the command-line interface for the ontology tool.

// Placeholder for future implementation
pub struct CLI;

impl CLI {
    /// Create a new CLI
    pub fn new() -> Self {
        Self
    }
} 